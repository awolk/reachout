import React, { Component } from 'react';

/**
 * Displays a map of US
 * Highlighted based on currently selected/hovered representatives
 */
export default class USMap extends Component {
  render() {
    return (
      <div/>
    );
  }
}